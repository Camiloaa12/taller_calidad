{
  "name": "taller_calidad",
  "version": "0.1.0",
  "private": true,
  "description": "Ejemplo mínimo para pruebas y análisis con SonarCloud",
  "scripts": {
    "test": "jest",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}